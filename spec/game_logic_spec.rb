require_relative "../lib/game_logic"
require "rspec"

describe "Gamelogic" do
  let(:@player2_trn) {[]}
  let(:@player1_trn) {[]}
  
  describe "cal_turns" do
    it "returns sum of arry" do

      expect(cal_turns).to eql([])
    end
  end
end